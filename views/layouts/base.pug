doctype html
html(lang="es")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Housing.com | #{title}
        link(rel="stylesheet", href="/css/app.css", attr)

    body(class="bg-gradient-to-b from-blue-800 to-blue-400").bg-no-repeat.bg-cover.min-h-screen

        if barra
            include header

        main.mx-auto.container
            block content